#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/Phase1Demo1
#    username: prod_user
#    password: CHANGE_ME
#
#  jpa:
#    hibernate:
#      ddl-auto: validate
#
#server:
#  port: 8080
#
#app:
#  jwt:
    # Must be at least 32 characters (256 bits) for HS256. Generate a real one with:
    # openssl rand -base64 32
#    secret: "Qm6t+uzx0HlQ+uXBW2k6UaYMa0GK3P/AoRgkN/HDC/M="
#    access-token-expiry-ms: 900000   # 15 minutes
#    refresh-token-expiry-ms: 604800000  # 7 days, used later when we build the refresh endpoint
spring:
  datasource:
    url: ${postgresql://evaap_db_user:bFgeWx7tX98y2BEjMuG9QmbDw5EvwmlB@dpg-d9e80gurnols73do34c0-a.singapore-postgres.render.com/evaap_db}
    username: ${evaap_db_user}
    password: ${bFgeWx7tX98y2BEjMuG9QmbDw5EvwmlB}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  port: 8080

app:
  jwt:
    secret: ${wMZTHG6t/T5sUAnIDto+CRZrAa3DZK1KpvdauF7xsq4=}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000
  oauth2:
    redirect-uri: ${OAUTH2_REDIRECT_URI:http://localhost:5173/auth/callback}