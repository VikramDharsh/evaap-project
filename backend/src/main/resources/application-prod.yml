spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Phase1Demo1
    username: prod_user
    password: CHANGE_ME

  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8080

app:
  jwt:
    # Must be at least 32 characters (256 bits) for HS256. Generate a real one with:
    # openssl rand -base64 32
    secret: "Qm6t+uzx0HlQ+uXBW2k6UaYMa0GK3P/AoRgkN/HDC/M="
    access-token-expiry-ms: 900000   # 15 minutes
    refresh-token-expiry-ms: 604800000  # 7 days, used later when we build the refresh endpoint
